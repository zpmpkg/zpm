registries:
  # - url: 'https://github.com/zpmpkg/registry.git'
  #   branch: 'master'
  - path: $ZPM/registry
registry:
  - name: extractor
    options:
      isBuildDefinition: true
      schema: $ZPM/extractors/extractor.v1.json
      defaults:
        libraries:
          name: ZPM
          path: extractors/default
  - name: builder
    options:
      isBuildDefinition: true
      defaults:
        libraries:
          name: ZPM
          path: builder/default
  - name: libraries
